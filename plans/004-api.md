# 004 — API

## Context

_TBD._

## Goal (demoable outcome)

`/api/team` route handlers run through Service and Repository. `isDarkSide` is a stub that returns `false` with a `TODO(006)`. Duplicate `POST` returns `409`. A sixth `POST` returns `422 TEAM_FULL`. Integration tests pass.

## Prerequisites

Slice 002 is done. Planning Phase 1's `contracts/team.openapi.yaml` is filled in.

## Steps

1. _TBD_

## Files touched

- _TBD_

## Verification

1. _TBD_

## Done criteria

- [ ] _TBD_
