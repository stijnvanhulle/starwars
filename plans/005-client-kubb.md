# 005 — Client (Kubb)

## Context

_TBD._

## Goal (demoable outcome)

`pnpm --filter platform gen` emits `src/gen/team/` (types, client, and Zod schemas) and `src/gen/starwars/` (types and Zod schemas). `store.ts` wires `starwarsApi` and `teamApi` into `<Providers>`.

## Prerequisites

Slice 003 and Slice 004 are done. Planning Phase 1's `contracts/starwars.openapi.yaml` is filled in.

## Steps

1. _TBD_

## Files touched

- _TBD_

## Verification

1. _TBD_

## Done criteria

- [ ] _TBD_
